<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Purchase model</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Purchase <%= model.name %></h1>
    <p><strong>Price:</strong> <%= model.price %> UN</p>

    <form action="/purchase/<%= model.id %>" method="POST">
        <label for="payment">Enter payment amount (UN):</label>
        <input type="number" name="payment" id="payment" value="<%= model.price %>" step="any" required>
        <button type="submit">Purchase</button>
    </form>

    <a href="/index">
        <img class="back" src="/next.png">
    </a>
</body>
</html>